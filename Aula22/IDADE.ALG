Algoritmo "idade"
//
Var
// Seção de Declarações das variáveis 
   idade : inteiro

Inicio
// Entrada de dados:
   escreva("Digite a sua idade: ")
   leia(idade)
   
// Processamento:
   se(idade >= 67) então
     escreval("Parabêns, você pode ser vacinado(a).")
   senão
     escreval("Vacina não disponivel no momento.")
   fimse
   
   escreval("Obrigado por acessar o sistema!")

Fimalgoritmo